type: object
properties:
  winner:
    type: string
    description: Team name; empty when resultKind != WIN.
  homeTeam:
    $ref: ./CricketScoreboardTeam.yaml
  awayTeam:
    $ref: ./CricketScoreboardTeam.yaml
