type: object
required:
  - username
  - passHash
properties:
  username:
    type: string
  passHash:
    type: string
    description: Client-side hashed password. The backend MUST re-hash before storing.
