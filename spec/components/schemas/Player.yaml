type: object
required:
  - slug
  - name
  - sport
  - status
  - createdAt
  - updatedAt
properties:
  slug:
    type: string
  name:
    type: string
  sport:
    $ref: ./SportWithOther.yaml
  status:
    $ref: ./PlayerStatus.yaml
  bio:
    type:
      - string
      - 'null'
  team:
    type:
      - string
      - 'null'
    description: >-
      Team name (stored as free text, not a Team.slug FK). The admin UI offers
      selection from saved teams — or inline creation of a new one — but persists
      the chosen team's name, and arbitrary free text is still accepted.
  position:
    type:
      - string
      - 'null'
  country:
    type:
      - string
      - 'null'
  photoUrl:
    type:
      - string
      - 'null'
    description: Public URL from /players/upload.
  jerseyNumber:
    type:
      - number
      - 'null'
    description: Last value seen on a match's homeTeamPlayers / awayTeamPlayers entry.
  createdAt:
    $ref: ./Timestamp.yaml
  updatedAt:
    $ref: ./Timestamp.yaml
