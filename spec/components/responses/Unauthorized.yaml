description: 'Missing, malformed, expired or revoked Firebase ID token (`Authorization: Bearer`).'
content:
  application/json:
    schema:
      $ref: ../schemas/Error.yaml
    example:
      error: Unauthorized
